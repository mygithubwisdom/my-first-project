// Create a student object
const student = {
    name: "Trump",
    age: 20,
    grade: "A"
  };
  console.log(student);

  // Function to greet the student
  function greetStudent(student) {
    console.log(`Hello, ${student.name}! Hope you're having a great day!`);
  }
  
  // Call the function to greet the student
  greetStudent(student);
 

  